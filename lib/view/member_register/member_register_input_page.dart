import 'package:flutter/material.dart';

class MemberRegisterInputPage extends StatelessWidget {
  const MemberRegisterInputPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
